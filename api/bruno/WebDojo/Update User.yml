info:
  name: Update User
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3333/api/users/9
  body:
    type: json
    data: |-
      {
        "name": "EKWQIOEO",
        "email": "jterwer1@teste.com",
        "password": "senhateste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
